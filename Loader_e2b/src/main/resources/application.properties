# Configurar la coneccion a la base de datos

spring.datasource.url = jdbc:mysql:/34.217.106.82:3306/asw2
spring.datasource.username = root
spring.datasource.password = asw
spring.datasource.driverClassName = com.mysql.jdbc.Driver

# Indicar el DBMS
spring.jpa.database: MYSQL

# Indica si debe mostrar el log de las consultas sql ejecutadas
# Bueno a la hora de depurar 
spring.jpa.show-sql: true

# Configurar Hibernate
# Cambiar esta propiedad a "update" para que no se cree una nueva
# db con cada ejecución
spring.jpa.hibernate.ddl-auto: create
spring.jpa.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy

